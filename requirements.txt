# git+https://github.com/huggingface/transformers
# accelerate
# datasets>=2.16.0,<=3.1.0
# accelerate>=0.34.0,<=1.0.1
# # peft>=0.11.1,<=0.12.0
# trl>=0.8.6,<=0.9.6
# tokenizers
# gradio>=4.38.0,<=5.12.0
# pandas>=2.0.0
# scipy
# einops
# sentencepiece
# tiktoken
# protobuf
# uvicorn
# pydantic
# fastapi
# sse-starlette
# matplotlib>=3.7.0
# fire
# packaging
# pyyaml
# numpy<2.0.0
# av
# tyro<0.9.0
# colorlog
# qwen-vl-utils[decord]
# torchvision
# unsloth[cu121-torch250] @ git+https://github.com/unslothai/unsloth.git
# nltk
# rouge-chinese
# python-dotenv
# bitsandbytes>=0.39.0
# ray[default]
# evaluate
# jiwer

# Install core requirements from requirements.txt style list
pip install \
git+https://github.com/huggingface/transformers \
accelerate \
datasets==3.2.0 \  # pinned to your override below
trl==0.9.6 \  # pinned to your override below
tokenizers \
gradio>=4.38.0,<=5.12.0 \
pandas>=2.0.0 \
scipy \
einops \
sentencepiece \
tiktoken \
protobuf \
uvicorn \
pydantic \
fastapi \
sse-starlette \
matplotlib>=3.7.0 \
fire \
packaging \
pyyaml \
numpy<2.0.0 \
av \
tyro<0.9.0 \
colorlog \  # included directly here
qwen-vl-utils[decord] \
torchvision \
unsloth[cu121-torch250] @ git+https://github.com/unslothai/unsloth.git \
nltk \
rouge-chinese \
python-dotenv \
bitsandbytes>=0.39.0 \
ray[default] \
evaluate \
jiwer

# Install additional pinned versions and commands explicitly
pip install peft==0.12.0
pip install dotenv
pip install qwen-vl-utils
pip install --upgrade transformers

# Export environment variable
export DISABLE_VERSION_CHECK=1
